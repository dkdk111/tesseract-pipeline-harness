# Normalize line endings to LF in the repository for all text files.
* text=auto eol=lf

# Explicit binary safety for common image types (none committed today, future-proof).
*.png binary
*.jpg binary
*.gif binary
